interface:
  display_name: "Loop Local Visual Studio"
  short_description: "Diagnose LLVS and collect privacy-safe feedback"
  default_prompt: "Use $llvs to diagnose this project's LLVS setup, inspect implemented capabilities, or record a local failure safely."
